{
  "permissions": {
    "allow": [
      "Bash(git push:*)",
      "Bash(ssh root@83.69.135.27 \"cd /opt/projects/analytics && git pull origin main && cd web && sudo rm -rf dist && npm run build && cd .. && sudo rm -rf /var/www/analytics/assets && sudo cp -r web/dist/* /var/www/analytics/ && docker compose build --no-cache api && docker compose up -d && docker restart nginx-proxy\")"
    ]
  }
}
